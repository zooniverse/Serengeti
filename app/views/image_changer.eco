<div class="images">
  <% for src, i in @sources: %>
    <figure><img src="<%= src %>" /></figure>
  <% end %>
</div>

<div class="toggles">
  <% for src, i in @sources: %>
    <button name="toggle" value="<%= i %>"><%= i + 1 %></button>
  <% end %>
</div>
